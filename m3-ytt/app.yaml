#@ name = "blue"
#@ image = "jpetazzo/color"
#@ port = 80
#@ labels = {"app": name, "deployed-by": "ytt"}
apiVersion: apps/v1
kind: Deployment
metadata:
  labels: #@ labels
  name: #@ name
spec:
  replicas: 1
  selector:
    matchLabels: #@ labels
  template:
    metadata:
      labels: #@ labels
    spec:
      containers:
      - image: #@ image
        name: #@ name
---
apiVersion: v1
kind: Service
metadata:
  labels: #@ labels
  name: #@ name
spec:
  ports:
  - name: #@ name
    port: #@ port
  selector: #@ labels
---
#@ name
